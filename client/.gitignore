node_modules/
dist/
dev-dist/
.env
.env.local
.env.*.local
*.local

# Self-hosted ML assets for visual search (~97 MB) — regenerated by
# `pnpm run fetch-ml-assets` (also runs automatically via the pre{dev,build}
# hooks). Not committed; see scripts/fetch-ml-assets.mjs.
public/ort/
public/models/
