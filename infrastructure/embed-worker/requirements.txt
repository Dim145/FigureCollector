# Image-embedding worker — deliberately light (no torch/CUDA). DINOv2-small q8
# runs fine on CPU via onnxruntime; a GPU box can add onnxruntime-gpu instead.
asyncpg==0.30.0
numpy==1.26.4
onnxruntime==1.20.1
Pillow==11.0.0
structlog==24.4.0
